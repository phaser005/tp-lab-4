import { SpecialityImgFormatterPipe } from './speciality-img-formatter.pipe';

describe('SpecialityImgFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new SpecialityImgFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
